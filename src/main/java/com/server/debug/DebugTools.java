package com.server.debug;

public class DebugTools {
    /**
     * delete this class its useless and if i chnage the.
     * method name it will damage lots of its uses i guess.
     * checkstyle needs to change its name.
     *
     * @param message string.
     */
    public static void debug(String message) {
        System.out.println("--Debug.DebugTools: " + message);
    }
}
